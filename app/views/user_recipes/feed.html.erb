<div class="feed-container">
  <div class="feed-header">
    <h1>Feeding the feed</h1>
    <div class="friends-toggle">
      <span class="feed feedSelected"><p>All</p></span>
      <span class="feed"><p>Friends</p></span>
    </div>
  </div>

  <div class="public-feed-container">
    <% @user_recipes.each do |ur| %>
      <%= link_to user_path(ur.user) do %>
        <div class="feed-card">
          <div class="feed-card-header">
            <% if ur.user.photo.attached? %>
              <%= cl_image_tag ur.user.photo.key, crop: :fill, class: 'user-img' %>
            <% else %>
              <%= image_tag "https://res.cloudinary.com/cuukin/image/upload/v1610757107/Cuukin%20-%20Avatar/ck_av_7_dkpysc.png", class: 'user-img' %>
            <% end %>
            <div class="card-header-text-info">
              <h5>@<%= ur.user.username %></h5>
              <p><%= "Lesson #{ur.recipe.lessons.first.book.id}.#{ur.recipe.lessons.first.order_in_book} | #{ur.recipe.title}" %></p>
            </div>
          </div>
          <div class="feed-image" style="background-image: linear-gradient(rgba(0,0,0,0), rgba(0,0,0,0)), url('<%= ur.photo.service_url %>')"></div>
        </div>
      <% end %>
    <% end %>
  </div>

  <div class="friends-feed-container"></div>
</div>
